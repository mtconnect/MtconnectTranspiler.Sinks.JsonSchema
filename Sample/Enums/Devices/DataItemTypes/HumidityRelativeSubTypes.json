{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "HumidityRelativeSubTypes",
  "type": "string",
  "enum": [
    "_COMMANDED",
    "_ACTUAL",
  ],
  "description": "",
  "properties": {
    "enumNames": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "enum": [
        "directive value including adjustments such as an offset or overrides.",
        "measured or reported value of an .",
      ]
    }
  }
}
