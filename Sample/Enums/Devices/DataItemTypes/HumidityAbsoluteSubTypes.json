{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "HumidityAbsoluteSubTypes",
  "type": "string",
  "enum": [
    "_ACTUAL",
    "_COMMANDED",
  ],
  "description": "",
  "properties": {
    "enumNames": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "enum": [
        "measured or reported value of an .",
        "directive value including adjustments such as an offset or overrides.",
      ]
    }
  }
}
