{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ControllerModeOverrideValues",
  "type": "string",
  "enum": [
    "ON",
    "OFF",
  ],
  "description": "",
  "properties": {
    "enumNames": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "enum": [
        " is in the  state and the mode override is active.",
        " is in the  state and the mode override is inactive.",
      ]
    }
  }
}
