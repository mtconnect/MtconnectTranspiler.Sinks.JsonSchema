{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PartCountTypeValues",
  "type": "string",
  "enum": [
    "EACH",
    "BATCH",
  ],
  "description": "",
  "properties": {
    "enumNames": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "enum": [
        "count is of individual items.",
        "pre-specified group of items.",
      ]
    }
  }
}
