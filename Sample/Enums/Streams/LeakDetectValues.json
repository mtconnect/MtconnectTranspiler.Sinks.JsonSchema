{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "LeakDetectValues",
  "type": "string",
  "enum": [
    "DETECTED",
    "NOT_DETECTED",
  ],
  "description": "",
  "properties": {
    "enumNames": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "enum": [
        "leak is currently being detected.",
        "leak is currently not being detected.",
      ]
    }
  }
}
