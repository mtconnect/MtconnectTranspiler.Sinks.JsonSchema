{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SpindleInterlockValues",
  "type": "string",
  "enum": [
    "ACTIVE",
    "INACTIVE",
  ],
  "description": "",
  "properties": {
    "enumNames": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "enum": [
        "power has been removed and the spindle cannot be operated.",
        "spindle has not been deactivated.",
      ]
    }
  }
}
