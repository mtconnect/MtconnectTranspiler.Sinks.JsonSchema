{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ChuckInterlockValues",
  "type": "string",
  "enum": [
    "ACTIVE",
    "INACTIVE",
  ],
  "description": "",
  "properties": {
    "enumNames": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "enum": [
        "chuck cannot be unclamped.",
        "chuck can be unclamped.",
      ]
    }
  }
}
