{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ControllerModeValues",
  "type": "string",
  "enum": [
    "AUTOMATIC",
    "MANUAL",
    "MANUAL_DATA_INPUT",
    "SEMI_AUTOMATIC",
    "EDIT",
    "FEED_HOLD",
  ],
  "description": "",
  "properties": {
    "enumNames": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "enum": [
        " is configured to automatically execute a program.",
        " is not executing an active program.   It is capable of receiving instructions from an external source – typically an operator. The  executes operations based on the instructions received from the external source.",
        "operator can enter a series of operations for the  to perform.  The  will execute this specific series of operations and then stop.",
        " is operating in a mode that restricts the active program from processing its next process step without operator intervention.",
        " is currently functioning as a programming device and is not capable of executing an active program.",
        "axes of the device are commanded to stop, but the spindle continues to function.",
      ]
    }
  }
}
