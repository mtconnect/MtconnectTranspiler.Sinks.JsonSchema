{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AvailabilityValues",
  "type": "string",
  "enum": [
    "AVAILABLE",
    "UNAVAILABLE",
  ],
  "description": "",
  "properties": {
    "enumNames": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "enum": [
        "data source is active and capable of providing data.",
        "data source is either inactive or not capable of providing data.",
      ]
    }
  }
}
