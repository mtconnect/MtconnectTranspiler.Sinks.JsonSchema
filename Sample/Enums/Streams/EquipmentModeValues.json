{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "EquipmentModeValues",
  "type": "string",
  "enum": [
    "ON",
    "OFF",
  ],
  "description": "",
  "properties": {
    "enumNames": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "enum": [
        "equipment is functioning in the mode designated by the .",
        "equipment is not functioning in the mode designated by the .",
      ]
    }
  }
}
