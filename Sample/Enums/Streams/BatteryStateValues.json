{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "BatteryStateValues",
  "type": "string",
  "enum": [
    "CHARGED",
    "CHARGING",
    "DISCHARGING",
    "DISCHARGED",
  ],
  "description": "",
  "properties": {
    "enumNames": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "enum": [
        " is at it's maximum rated charge level.",
        "'s charge is increasing.",
        "'s charge is decreasing.",
        " is at it's minimum charge level.",
      ]
    }
  }
}
